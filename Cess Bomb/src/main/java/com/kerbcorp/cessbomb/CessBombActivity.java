package com.kerbcorp.cessbomb; // Keep whatever package name is already at the top of your file

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class CessBombActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);

        // This is where your teammates will eventually link the GameView engine!
    }
}